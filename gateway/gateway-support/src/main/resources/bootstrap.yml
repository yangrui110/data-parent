spring:
  application:
    name: gateway
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        group: gateway-support
        file-extension: yml
        server-addr: 127.0.0.1:8848
      discovery:
        server-addr: 127.0.0.1:8848